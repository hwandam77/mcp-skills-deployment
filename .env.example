# MCP + Skills 배포 패키지 환경 변수 템플릿
#
# 사용법:
#   1. cp .env.example .env
#   2. vim .env (실제 값으로 편집)
#   3. chmod 600 .env (권한 설정)

# ============================================
# Database Configuration (Knowledge Base MCP)
# ============================================
DB_HOST=your-database-host
DB_PORT=5432
DB_NAME=your-database-name
DB_USER=your-database-user
DB_PASSWORD=your-secure-password

# ============================================
# MCP Server Configuration
# ============================================
KNOWLEDGE_MCP_HOST=127.0.0.1
KNOWLEDGE_MCP_PORT=8711

# ============================================
# API Keys (선택 - 필요한 MCP에 따라)
# ============================================

# Slack MCP
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_TEAM_ID=your-slack-team-id

# Brave Search MCP
BRAVE_API_KEY=your-brave-api-key

# GitHub MCP
GITHUB_TOKEN=your-github-personal-access-token

# Google Drive MCP
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REFRESH_TOKEN=your-google-refresh-token

# ============================================
# AI Models (Ollama - 선택)
# ============================================
OLLAMA_HOST=http://localhost:11434
EMBEDDING_MODEL=bge-m3
LLM_MODEL=gemma3

# ============================================
# Logging & Monitoring (선택)
# ============================================
LOG_LEVEL=info
LOG_FILE=/var/log/mcp/server.log

# ============================================
# SSH Configuration (SSH MCP - 선택)
# ============================================
SSH_DEFAULT_PORT=22
SSH_TIMEOUT=30000

# ============================================
# 주의사항
# ============================================
# 1. 이 파일(.env)을 Git에 커밋하지 마세요!
# 2. .gitignore에 .env가 포함되어 있는지 확인하세요
# 3. 실제 값으로 편집 후 chmod 600 .env로 권한 설정
# 4. 프로덕션 환경에서는 HashiCorp Vault 등 사용 권장
